{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "NITRO_PRESET=cloudflare yarn build && wrangler publish"
  },
  "devDependencies": {
    "@nuxt/content": "^2.4.3",
    "@nuxt/devtools": "^0.1.2",
    "@types/node": "^18.13.0",
    "nuxt": "^3.2.0",
    "nuxt-og-image": "^1.4.13",
    "postcss-nested": "^6.0.0"
  }
}
